{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "sourceMap": false
  },
  // src más los tests y la config de Vitest: si un archivo no pertenece al
  // programa de ningún tsconfig, SonarCloud lo indexa pero no lo analiza.
  "include": ["src", "test", "vitest.config.ts"]
}

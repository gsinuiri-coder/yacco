# Estado por módulo

Un solo lugar para ver qué dominio tiene endpoints, cuáles tienen pantalla en
la web y cuáles no. Existe porque su ausencia ya hizo planificar trabajo que
en realidad estaba hecho.

**Regla central: esta tabla se deriva del código, no de otra documentación.**
Se arma recorriendo `apps/api/src/modules/**/*.controller.ts` (prefijo de
`@Controller` + método y ruta de cada `@Get`/`@Post`/`@Patch`/`@Delete`),
`apps/web/src/app.tsx` (árbol de rutas real) y `apps/web/src/pages/` +
`apps/web/src/api/` (qué pantalla existe y qué cliente HTTP la alimenta). Si
el código y algún documento se contradicen, gana el código.

Convención de rutas para no repetirlas en cada celda: el controller de un
dominio vive en `apps/api/src/modules/<dominio>/<dominio>.controller.ts`; sus
páginas web, en `apps/web/src/pages/<página>.tsx`. Toda ruta de API omite el
prefijo `/api/v1` salvo que se indique lo contrario (`health` queda afuera de
ese prefijo a propósito, ver `apps/api/src/main.ts`).

## Estados

- **Completo** — hay endpoints y hay una pantalla en `apps/web` que los
  consume (propia o embebida dentro de otra).
- **Solo API** — los endpoints existen; no hay nada en `apps/web` que los
  llame.
- **Interno** — el módulo no tiene `controller`; se invoca desde otro
  servicio o desde el CLI.

## Tabla

| Dominio                    | Endpoints                                                                                                                                                                                                                                                                                                          | Pantalla web                                                                                                                                                                                                    | Estado                                                                                                    |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `auth`                     | `POST /auth/login`<br>`POST /auth/refresh`                                                                                                                                                                                                                                                                         | `login-page.tsx` (login); el refresco lo hace `api/api-client.ts` en cada pantalla protegida, sin pantalla propia                                                                                               | Completo                                                                                                  |
| `container-balances`       | `GET /container-balances`                                                                                                                                                                                                                                                                                          | `container-counts-page.tsx`                                                                                                                                                                                     | Completo                                                                                                  |
| `container-counts`         | `POST /container-counts`                                                                                                                                                                                                                                                                                           | `components/container-count-form.tsx`, embebido en `container-counts-page.tsx`                                                                                                                                  | Completo                                                                                                  |
| `container-movements`      | `POST /container-movements`<br>`GET /container-movements`<br>`GET /container-movements/inventory`                                                                                                                                                                                                                  | `container-movements-page.tsx` (alta e historial); `GET .../inventory` lo consume `inventory-page.tsx`                                                                                                          | Completo                                                                                                  |
| `container-reconciliation` | `GET /container-reconciliation`                                                                                                                                                                                                                                                                                    | Ninguna. Sin cliente en `apps/web/src/api`                                                                                                                                                                      | Solo API                                                                                                  |
| `container-types`          | `POST /container-types`<br>`GET /container-types`<br>`GET /container-types/:id`<br>`PATCH /container-types/:id`                                                                                                                                                                                                    | `container-types-page.tsx` (alta, listado, renombrar, retirar); catálogo reusado dentro de `container-movements-page.tsx`, `container-counts-page.tsx` y `production-page.tsx`                                  | Completo — `GET /:id` sin cliente en `apps/web/src/api/container-types.ts`, ver reporte                   |
| `customer-locations`       | `GET /customers/:customerId/locations`                                                                                                                                                                                                                                                                             | Sin pantalla propia; se consume dentro de `container-movements-page.tsx` (selector de locación al registrar un movimiento hacia/desde el cliente)                                                               | Completo                                                                                                  |
| `customer-prices`          | `GET /customers/:customerId/prices`<br>`POST /customers/:customerId/prices`<br>`PATCH /customers/:customerId/prices/:priceId`<br>`DELETE /customers/:customerId/prices/:priceId`<br>`GET /customers/:customerId/effective-prices`                                                                                  | Sin pantalla propia; se consume dentro de `components/customer-prices-section.tsx` (en `customer-detail-page.tsx`) y de `components/order-items-form.tsx` (en `order-create-page.tsx`, solo `effective-prices`) | Completo                                                                                                  |
| `customers`                | `POST /customers`<br>`GET /customers`<br>`GET /customers/:id`<br>`PATCH /customers/:id`<br>`GET /customers/:id/account-statement`                                                                                                                                                                                  | `customers-page.tsx`, `customer-create-page.tsx`, `customer-detail-page.tsx`, `customer-edit-page.tsx`                                                                                                          | Completo — `account-statement` sin cliente en `apps/web/src/api` y sin pantalla que lo llame, ver reporte |
| `health`                   | `GET /health` (fuera de `/api/v1`)<br>`GET /health/db` (fuera de `/api/v1`)                                                                                                                                                                                                                                        | Ninguna. Endpoint de infraestructura: lo consume el healthcheck de Render, no la web                                                                                                                            | Solo API                                                                                                  |
| `orders`                   | `POST /orders`<br>`GET /orders`<br>`GET /orders/:id`<br>`PATCH /orders/:id/cancel`                                                                                                                                                                                                                                 | `orders-page.tsx`, `order-create-page.tsx`, `order-detail-page.tsx`                                                                                                                                             | Completo                                                                                                  |
| `payment-methods`          | `GET /payment-methods`<br>`GET /payment-methods/:id`                                                                                                                                                                                                                                                               | Ninguna. Sin cliente en `apps/web/src/api`                                                                                                                                                                      | Solo API                                                                                                  |
| `payments`                 | `POST /payments`<br>`GET /payments`<br>`POST /payments/:id/confirm`<br>`POST /payments/:id/reject`                                                                                                                                                                                                                 | Ninguna. Sin cliente en `apps/web/src/api`                                                                                                                                                                      | Solo API                                                                                                  |
| `production-batches`       | `POST /production-batches`<br>`GET /production-batches`<br>`GET /production-batches/:id`                                                                                                                                                                                                                           | `production-page.tsx` (alta y listado)                                                                                                                                                                          | Completo — `GET /:id` sin cliente en `apps/web/src/api/production-batches.ts`, ver reporte                |
| `products`                 | `GET /products`                                                                                                                                                                                                                                                                                                    | Sin pantalla propia; se consume dentro de `components/order-items-form.tsx` y `components/customer-prices-section.tsx`                                                                                          | Completo                                                                                                  |
| `roster-loader`            | Sin controller. Se invoca desde `apps/api/src/cli/load-roster.ts` (script `load:roster`)                                                                                                                                                                                                                           | Ninguna                                                                                                                                                                                                         | Interno                                                                                                   |
| `routes`                   | `POST /routes`<br>`GET /routes`<br>`GET /routes/:id`<br>`PATCH /routes/:id/start`<br>`PATCH /routes/:id/finish`<br>`POST /routes/:id/stops`<br>`PATCH /routes/:id/stops/reorder`<br>`PATCH /routes/:id/stops/:stopId`<br>`POST /routes/:id/loads`<br>`GET /routes/:id/loads`<br>`DELETE /routes/:id/loads/:loadId` | Ninguna. Sin cliente en `apps/web/src/api`                                                                                                                                                                      | Solo API                                                                                                  |
| `route-settlement`         | `GET /routes/:routeId/settlement`<br>`POST /routes/:routeId/settlement`                                                                                                                                                                                                                                            | Ninguna. Sin cliente en `apps/web/src/api`                                                                                                                                                                      | Solo API                                                                                                  |
| `sales`                    | Sin controller. Se invoca desde `RoutesService.markStop` (`routes/routes.service.ts`) y desde `roster-loader/roster-loader.service.ts`                                                                                                                                                                             | Ninguna                                                                                                                                                                                                         | Interno                                                                                                   |
| `users`                    | `GET /users`<br>`POST /users`<br>`PATCH /users/:id`                                                                                                                                                                                                                                                                | Ninguna. Sin cliente en `apps/web/src/api`                                                                                                                                                                      | Solo API                                                                                                  |
| `zones`                    | `POST /zones`<br>`GET /zones`<br>`GET /zones/:id`<br>`PATCH /zones/:id`                                                                                                                                                                                                                                            | Ninguna. Sin cliente en `apps/web/src/api`                                                                                                                                                                      | Solo API                                                                                                  |

21 dominios — coincide con los 21 directorios de `apps/api/src/modules`.

## Cómo se actualiza

Se regenera recorriendo el código, nunca a mano contra la memoria de quien la
edita: `docs/estado-por-modulo.md` es un derivado, no una fuente. La skill
`sprint-close` la reconstruye como paso de su checklist (ver
`.claude/skills/sprint-close/SKILL.md`). Si al regenerarla una fila cambia de
estado o aparece/desaparece un dominio, es información real sobre el sprint
— repórtalo en el acta de cierre, no lo escondas ajustando la tabla en
silencio.
